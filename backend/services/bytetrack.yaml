# ByteTrack configuration — tuned for drone/overhead traffic footage
tracker_type: bytetrack

track_high_thresh: 0.5
track_low_thresh: 0.1
new_track_thresh: 0.5
track_buffer: 50
match_thresh: 0.8
fuse_score: True
